export type DashboardInsights = {
  insights: string[];
};

export const generateInsights = async (): Promise<DashboardInsights> => {
  throw new Error("generateInsights is not wired in this codebase yet");
};
