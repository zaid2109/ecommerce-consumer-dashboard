export type DashboardMetrics = Record<string, unknown>;

export const calculateMetrics = async (): Promise<DashboardMetrics> => {
  throw new Error("calculateMetrics is not wired in this codebase yet");
};
